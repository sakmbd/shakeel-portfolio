// Fraunces display serif — two static weights only (latin subset, ~20KB
// WOFF2 each, font-display: swap by default) for the name/heading hierarchy.
// Body/UI text stays on the system sans stack for zero additional cost.
import '@fontsource/fraunces/latin-500.css';
import '@fontsource/fraunces/latin-700.css';
